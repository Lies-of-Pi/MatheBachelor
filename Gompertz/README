Die R-Datei data_generation_gompertz erzeugt die Daten und nötigen Werte für das wahre Modell der Zustandsbesetzungswahrscheinlichkeit.
Außerdem wird ein Histogramm der Daten erzeugt um die Verteilung zu veranschaulichen. Beispieldaten können der Datei Beispieldaten entnommen werden.
Um mein Analytisches Modell zu testen habe ich die Variable just_one_covariate in data_generation_gompertz implementiert. Wird diese auf True gesetzt,
so werden nur Daten für einen festen Patienten erzeugt. Dabei kann man nun über die Daten direkt die Zustandsbesetzungswahrscheinlichkeit empirisch auszählen.
Sobald der Parameter wieder auf False gesetzt wird, werden wieder Daten verschiedener Patienten erzeugt.
Um die Abbildungen der Datengenerierung aus der Arbeit zu erhalten, muss in der Datei data_generation_gompertz just_one_covariate auf True gesetzt werden. Daraufhin werden die
Daten und die Werte des wahren analytischen Modells berechnet. Die Datei True_State_Prob_Load_and_Plot plottet dann die Ergebnisse und erzeugt die besagten Abbildungen.
Diese können dem Ordner StateProbTheory entnommen werden.
Um den Plot mit dem Test der Konsistenz des Partial Likelihood Schätzers zu generieren, muss man zunächst mit data_generation_gompertz
passende Daten generieren. Daraufhin kann man den Code in test_partial_likelihood_consistence ausführen, der den Test durchführt.
